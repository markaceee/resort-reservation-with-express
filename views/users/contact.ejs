<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/styles.css">
    
    <!-- FONTAWESOME ICONS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <!-- BOOTSTRAP ICONS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.4/font/bootstrap-icons.css">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <title>Contact Us</title>
</head>
<body>
    <%- include('plugins/navbar') %>

    <section class="contact">
        <div class="header">
            <h1>Contact information</h1>
            <p>Fill up the form and we will get back to you within 24 hours.</p>
        </div>
        <div class="box-container">
            <div class="image">
                <img src="images/myBookings.png" alt="">
            </div>
            <form action="/contact" id="contactForm" method="post">
                <div class="labelBox">
                    <label for="fname">First name</label>
                    <input type="text" id="fname" placeholder="Enter your first name" name="firstName"  <% if(isAuthenticated){%> value="<%= data[0].firstName %>" <%}%>  required>
                </div>
                <div class="labelBox">
                    <label for="lname">Last name</label>
                    <input type="text" id="lname" placeholder="Enter your last name" name="lastName" <% if(isAuthenticated){%> value="<%= data[0].lastName %>" <%}%> required>
                </div>
                <div class="labelBox">
                    <label for="email">Email</label>
                    <input type="email" id="email" placeholder="Enter your email" name="email" <% if(isAuthenticated){%> value="<%= data[0].email %>" <%}%> required>
                </div>
                <div class="labelBox">
                    <label for="contactNo">Contact Number</label>
                    <input type="number" id="contactNo" placeholder="Enter your contact number" name="contactNumber" <% if(isAuthenticated){%> value="<%= data[0].contactNumber %>" <%}%> required>
                </div>
                <div class="labelBox">
                    <label for="message">Enter message</label>
                    <textarea id="message" rows="4" placeholder="Enter your message" name="message" required></textarea>
                </div>
                <button type="submit">Send message</button>
            </form>
        </div>
    </section>
    
    <script>
    let success = '<%= success %>';
    if(success.length > 0){
        Swal.fire({
            position: 'top-end',
            icon: 'success',
            title: 'Message sent!',
            showConfirmButton: false,
            timer: 3000
        })
    }
    </script>
    
 
<%- include('plugins/footer') %>